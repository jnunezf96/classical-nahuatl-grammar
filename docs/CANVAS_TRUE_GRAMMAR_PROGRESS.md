# Canvas true grammar progress

Strict completion: **NO**.

This report deliberately has no weighted overall percentage. An atom-to-owner link does not count as exact behavior.

## Current measurements

| Denominator | Done | Total | Progress | Status |
|---|---:|---:|---:|---|
| fullCanvasSourceZones | 58 | 66 | 87.88% | incomplete |
| lessonSourceZones | 58 | 58 | 100.00% | complete |
| lessonAtomization | 28540 | 28540 | 100.00% | complete |
| lessonAtomForceClassification | 28540 | 28540 | 100.00% | complete |
| lessonNonGrammarAccounting | 9901 | 9901 | 100.00% | complete |
| lessonGrammarOwnerLinked | 18639 | 18639 | 100.00% | complete |
| lessonGrammarExactBehaviorObserved | 6256 | 18639 | 33.56% | incomplete |
| applicationAxisClassification | 441 | 441 | 100.00% | complete |
| genuineChoiceCanvasProvenance | 66 | 66 | 100.00% | complete |
| genuineChoiceLiveDeclarations | 32 | 66 | 48.48% | incomplete |
| genuineChoiceBrowserDeliveryProof | not measured | 66 | not measured | not-measured |

## Exact behavior rule

Lessons 1–58 contain **18,639** grammar-bearing atoms. All have owner links, but only **6,256** currently have an acceptable exact-behavior observation.

The remaining **12,383** use non-generative assertion coordinates where the canonical engine must instead be observed performing, deriving, projecting, enforcing, constructing, or validating the atom-specific behavior.

A legitimately read-only grammar fact may remain non-generative only when a test observes its exact typed value. Evidence, analysis, and documentary material never authorize or block grammar.

## Full-Canvas scope blockers

The present atom denominator covers Lessons 1–58, not all source zones in the Canvas file:

- Front matter
- Appendix A
- Appendix B
- Appendix C
- Appendix D
- Appendix E
- Appendix F
- Suggested Reading

The 87.88% source-zone figure is structural only; it is not grammatical completion.

## Genuine-choice delivery backlog

All 66 genuine application axes are classified and linked to Canvas. 32 are live-declared; 34 remain contract-audited:

- `vnc:ordered-voice-chain` / `voice-operation-order`
- `vnc:ordered-voice-application` / `voice-operation-order`
- `nnc:deverbal-construction` / `nominalization-family`
- `nnc:deverbal-construction` / `patientive-family`
- `nnc:deverbal-construction` / `external-object`
- `nnc:deverbal-construction` / `double-nucleus-ownerhood`
- `nnc:adjectival-modification` / `modification-topology`
- `nnc:adjectival-modification` / `modifier-head-order`
- `nnc:adjectival-modification` / `adjunctor`
- `nnc:adjectival-modification` / `transitive-reference-contact`
- `nnc:adjectival-modification` / `compound-head-target`
- `nnc:adverbial` / `adverbial-context`
- `nnc:adverbial` / `clause-rank`
- `nnc:relational` / `relation-family`
- `nnc:relational` / `possessor-structure`
- `nnc:place-gentilic` / `place-formation`
- `nnc:place-gentilic` / `gentilic-formation`
- `nnc:place-gentilic` / `collectivity`
- `clause:adverbial-adjunction` / `relation-scope`
- `clause:adverbial-adjunction` / `clause-position`
- `clause:composition` / `complement-relation`
- `clause:composition` / `conjunction-relation`
- `clause:composition` / `clause-rank`
- `clause:composition` / `relation-marker`
- `clause:composition` / `parallel-structure`
- `clause:comparison` / `degree-strategy`
- `clause:comparison` / `superlative-strategy`
- `vnc:denominal` / `denominal-operation`
- `vnc:denominal` / `target-verbstem-class`
- `vnc:denominal` / `finite-participants`
- `nnc:personal-name` / `name-source-family`
- `nnc:personal-name` / `outer-subject`
- `nnc:personal-name` / `sentence-operation`
- `nnc:personal-name` / `reranking`

## Completion rule

Strict completion requires an atom-specific test to observe the canonical engine performing or enforcing every executable grammar contribution. Owner linkage, copied expectations, and non-generative assertions cannot satisfy that obligation.

## Focused closure: §24.5 opening (2026-09-04)

Build `20260904-destockal-harmony-owner-412` corrects the canonical long-stock
harmony analysis: short root `e` normally selects `ō`; `hui` also permits `ē`.
Long root vowels remain admitted without being claimed as instances of the
normal short-root rule. Signed lexical exceptions and open candidates requiring
Source-analysis selection have distinct readouts; neither examples nor prevalence
become an admission gate.

Owner `classical-destockal-stock-vowel-harmony` now gives direct observations for
`ACI-P201-L004-8CF59410D7`, the complete structured claim
`ACI-P201-L004-5D859597BD`, and `ACI-P201-L005-2E368A61C9`.
The descriptive prevalence atom `ACI-P201-L004-5C4E0532B7` remains unreceipted.
The focused `classical_destockal_stock_vowel_harmony_owner_exact` proof passed
23/23 checks. The live app also generated `Nipeyōni` from typed `pey-ō-ni`, Class B.

This concrete repair is closed. Global totals above were not regenerated.
The suspected short-stock cases were resolved by the user's OCR corrections in
the following block. The previously observed aggregate validation status
discrepancy is separate factory backlog, not a failure of these selection-specific
owner receipts.

## Focused closure: corrected ni-stem quantities (2026-09-04)

The user corrected the OCR readings to **`tlatz-ī-ni`** and **`ōl-ī-ni`**.
Both have long stock `ī`; neither is a short-stock exception. These corrections
are applied to the Canvas, matching inventory text, runtime lexical facts, and
the relevant source fixtures. Permanent atom IDs and source line coordinates
are preserved.

Build `20260904-ni-stock-ocr-corrections-414` retains `tlatz-ī-ni` as a lexical
vowel-harmony exception and `ōl-ī-ni` as a long-root source outside the normal
short-root harmony domain. Source analysis and Result agree on long `ī`.
The causative retains `ōl-ī-ni-ā`; impersonal `tla-tlatz-ī-ni` uses ordinary
prefixation, not an invented stock-lengthening operation. The temporary
short-stock implementation and its unfinished proof were removed.

Owner `classical-destockal-ni-hui-lexical-inventory` now directly observes
`ACI-P201-L023-AF06CA7748` and `ACI-P202-L018-E2B0E76F50` with its `-02`, `-03`,
and `-04` facets. The focused `classical_ni_stock_ocr_corrections_exact` proof
passed 18/18 checks. The live app generated `Nitlatzīni` and `Nōlīni`.

This correction block is closed. Global accounting and historical/generated
evidence ledgers were not regenerated; that factory work does not block these
corrected product results.

## Focused closure: Class B stock preservation (2026-09-04)

Build `20260904-class-b-stock-preservation-415` removes the erroneous exact
`chay-ā-hui` perfective override that shortened its stock vowel. The existing
canonical Class B owner now performs final-vowel loss and `hu → uh`, preserving
`ā`: `chay-ā-hui → chay-ā-uh`. No replacement lexical exception was added.

Canvas §24.5.6 supplies the rule atom `ACI-P203-L004-321DD44966`; its `-02/-03`
examples remain witnesses, not source-admission permissions or direct atom
receipts. The immediate §24.5.8 boundary (`ACI-P203-L022-3EF3B0BE62` and
`ACI-P203-L022-F1EA346E69`) retains signed replacement Class B and addition
Class C causatives.

The focused `classical_destockal_class_b_stock_preservation_exact` proof passed
10/10 checks: canonical finite formula/surface, ni/hui neighbors, unlisted long
and short stocks, both causative procedures, and copied-authority rejection.
The live browser, with Class B and preterit selected, generated **Nichayāuh.**

This product repair is closed. The class-system owner specifications still use
legacy example-proxy observation paths; replacing those with structured direct
atom observations remains separate, non-blocking owner-proof backlog. No global
totals, historical ledgers, or aggregate validation status were regenerated.

## Focused closure: ni/hui class-system owner observations (2026-09-04)

Build `20260904-destockal-class-owner-416` completes the separate owner-proof
action identified above. Normal long-stock ni/hui source analyses now carry one
shared class relation: intransitive source B, replacement `n-a`/`hu-a` B, and
addition `ni-ā`/`hui-ā` C. Actual Type 1 options consume that relation using their
signed source-analysis identity and typed target construction. Class remains a
contextual fact, not a new choice inferred from a surface suffix.

Owners `classical-destockal-ni-hui-class-system` and
`classical-destockal-causative-class-system` now provide structured direct
observations for `ACI-P203-L004-321DD44966`, `ACI-P203-L005-AC982449B7`,
`ACI-P203-L022-3EF3B0BE62`, and `ACI-P203-L022-F1EA346E69`. All four legacy
claim/facet identities remain resolvable at the corresponding structured
observation. Example children and the cross-reference child remain
non-authorizing; no new receipts were assigned to them.

Both owners share one focused, owner-issued validation result. It observes
canonical source-specific options and preferences for two unlisted sources,
`cot-ō-ni`, and `tlap-ī-hui`; it does not assert unrestricted free alternation
for every lexical source. It neither invokes nor repairs the separate aggregate
validation result. Fused and short-stock derivations, Class A candidates, and
Type 2 operations do not acquire the normal Type 1 class-assignment relation.

The focused `classical_destockal_class_system_owner_exact` proof passed 19/19
checks, including four direct observations, four legacy aliases, actual finite
Results, lexical alternatives, and copied-authority rejection. The first run
exposed a test that accidentally included Type 2 options; the final proof
separates and explicitly checks that boundary. The live browser retained both
Type 1 procedures and generated **Nēchchayāhuia.** with addition selected and
Class C shown in the analysis.

This owner-proof action is closed. Global totals, historical evidence, and the
previously recorded aggregate-status discrepancy remain untouched factory
backlog; this focused proof does not claim repository-wide closure.

## Focused closure: corrected ē-stock hua/hui reconstructions (2026-09-04)

The user corrected the OCR reconstruction to **`ce-ē-hui`** and clarified:
retain **`ce-ē-hua`** and **`e-ē-hua`**, with the corresponding **`-hui`**
variants. The proposed short-e reconstruction was stopped before implementation.
Canvas §24.5.9 and the two matching inventory texts
`ACI-P204-L004-AE01B03F8B` / `-02` now retain long stock `ē`; permanent IDs and
source coordinates are unchanged. The §24.6.2 `-hua` passage remains intact.

Build `20260904-long-e-hua-hui-417` admits `ce-ē-hui` as the same signed lexical
reconstruction and exact causative source as `cē-hui`. Its analysis continues
to be `ce` + `ē` + `hui`. The existing canonical boundary owner now realizes
long-ē + hui contraction, including the Class B perfective `uh` shape. This
works for typed `ce-ē-hui → cēhui`, `e-ē-hui → ēhui`, and unlisted matching
roots without using example membership as permission. Existing
`ce-ē-hua → cēhua` and `e-ē-hua → ēhua` remain correct. Formula retains its
typed morphemes independently of the contracted written projection.

The focused `classical_long_e_hua_hui_reconstruction_exact` proof passed 16/16
checks: Source parts, both hua/hui formations, perfectives, open input, signed
exact causative continuity, nonmatching/short-stock boundaries, and tampered
output rejection. An initial test incorrectly assumed the content-validated
surface projection rejected unchanged copies; the final proof tests edited
surface content and copied identity-bound application authority instead.
Live browser results were **Nicēhui.** and **Nēhui.**

This correction block is closed. It adds no general lifecycle receipts.
The general fused-stock coalescence observations and the nonextant `po/to`
source lifecycle remain separate owner-proof backlog: the existing
claim-p2344 `xīni` route observation does not prove nonextant-source behavior.
Global totals and historical/generated evidence were not refreshed.

## Focused closure: typed root/stock coalescence owner (2026-09-04)

Build `20260904-destockal-coalescence-418` replaces the selected formative
combinations in the canonical written-boundary owner with one typed relation:
a short root-final vowel and a long stock vowel of the same quality coalesce
into one long vowel. The relation covers ni/hui and their Class B finite
forms n/uh, without exact-stem membership. Existing long-ē + hua behavior is
retained under its own §24.6.2 family. Source admission and class selection
are unchanged; Formula retains the underlying morphemes independently of
Surface. The boundary carries the shared rule, actual condition, 2-to-1
realization, and resulting stock.

Owner `classical-destockal-irregular-source-lifecycle` now directly observes
four general §24.5.9 atoms: `ACI-P203-L023-C42FB7761B`,
`ACI-P203-L023-4A6F6CC3BE`, `-02`, and `-03`. Its permanent claim-p2343
identity now points to that structured observation instead of the mīni
example route. The focused projection observes canonical applications in
both formative families across all four vowel qualities; lexical/example
children acquire no general-rule receipts.

The false claim-p2344 xīni witness has been removed. That legacy Source
request remains resolvable, but requests its separate nonextant-source
capability and receives a blocked Result with no completed owner operation.
Its trace may correctly document the blocked attempt; it is not evidence
that the lifecycle claim has been proved. No po/to lifecycle semantics or
direct atom receipts were invented.

The focused `classical_destockal_coalescence_owner_exact` proof passed 17/17
checks, including four direct observations, legacy compatibility, productive
vowel coverage, present/perfective Results, excluded boundaries, retained hua,
and copied/tampered authority rejection. Test-only corrections respected the
private validation interface, documentary-metadata stripping, and valid
blocked-attempt traces; runtime behavior was not weakened to satisfy them.
The live browser generated **Nimīni.** from `mi-ī-ni` with Class B selected.

This coalescence block is closed. The separate nonextant po/to lifecycle and
previously recorded aggregate-status discrepancy remain non-blocking backlog.
No global totals, historical ledgers, or generated evidence were refreshed.

## Focused closure: nonextant po/to source lineage (2026-09-04)

Build `20260904-nonextant-source-lifecycle-419` completes the separate
§24.5.9 dependency atom `ACI-P204-L005-94CD718F68`. The permanent claim-p2344
and its new direct coordinate both observe
`constraints.destockalIrregularSourceLifecycle.nonextantNiSourceLifecycle`.
The observation exhausts a two-entry typed historical source system: root
`po` or `to`, stock `ō`, theme `ni`, independent-use status `nonextant`, and
reconstructed-source status. It no longer borrows an xīni causative or a
completion flag as evidence.

In the existing lexicalized frequentative operation, exact surviving
`po-pō-ca` and `to-tō-ca` lexical identities select their internal historical
sources. The canonical operation builds the target from those typed morphemes:
shared root/stock coalescence, short-vowel reduplication, and replacement of
ni by ca. This replaces passthrough for those identities. The coalescence
relation now lives in one shared grammar module also consumed by ordinary
finite boundaries; neither consumer reads a rendered formula or written
answer to decide morphology.

Other completed typed sources remain open and receive no exact po/to lineage.
The prior blanket `extinct-or-fused-destockal` history assertion is no longer
issued from completed shape alone. Historical nonextant status is not a new
Source-admission gate, nor a claim that arbitrary additional derivations from
a reconstructed source have been established. This block proves internal
ancestry of the two surviving frequentatives; it does not change independent
Source-input policy or implement universal fused-stock frequentative formation.
The §24.5.9 cross-reference and example children remain non-authorizing, and
the separate §27.4.3 lexical/formation/realization atoms acquire no receipts.

The focused `classical_nonextant_destockal_source_lifecycle_exact` proof passed
14/14 on its first run: direct and legacy owner observations, exact two-source
coverage, shared-rule identity, typed derivation continuity, independent
formula/surface projections, Class A preterits, ordinary stock reduction,
unknown-source boundaries, injected-lineage rejection, and copied-authority
rejection. The former blocked-state expectation in the earlier coalescence
test was updated to this newly implemented separate dependency; that closed
suite was not rerun. The live browser selected **extinct or fused destockal**
and generated **Nipopōca.**

This lineage block is closed. Broader frequentative owner observations and
the previously recorded aggregate-status discrepancy remain non-blocking
backlog. No global totals, historical ledgers, or generated evidence were
refreshed.

## Focused closure: fused-stock frequentative formation and pi correction (2026-09-04)

Build `20260904-fused-frequentative-420` implements the general §27.4.3
long-vowel retention rule for both intransitive ca and causative tz-a
frequentatives. Canonical typed root/stock coalescence now drives their target
formation; the ordinary non-fused stock-reduction path remains unchanged.
Intransitive sources use owner-issued source analyses, including the existing
lexical reconstructions for mī-ni and cē-hui. Causatives use the selected
destockal construction and the engine-issued source morphology, matching the
parsed root/stock/suffix to the signed source without treating editorial
hyphens as authority. Both paths consume the same shared coalescence relation.

The actual operation carries `fusedStockFrequentativeFrame`: source agreement,
source analysis where available, source coalescence, a shared two-family rule,
and the target constructed from copied prefix, retained fused stock, and
ca/tz-a. Repetition and ordinary finite Class A/B behavior remain downstream.
Neither a long vowel by itself nor a caller-supplied relation establishes
fusion; unresolved contracted causative ancestry is not invented.

Owner `classical-fused-stock-vowel-frequentative` now gives direct structured
observations for `ACI-P247-L033-8EEEAAC63F` (formation scope) and
`ACI-P247-L034-5663D65B8D` (long-vowel retention). Permanent p2700/p2701
identities resolve to those exact observations. Their focused projection
observes seven canonical operations spanning four vowel qualities, ni/hui,
and the existing n-a/ni-ā/hu-a causative shapes. The separate lexical claims
p2702–p2704 and documentary/example children receive no new receipts here.

During this block the user corrected the OCR form to **pi-pī-tza** and made
the authority boundary explicit: Canvas determines exceptions; evidence may
contain OCR mismatches. Canvas line 9212 and the matching text of
`ACI-P248-L003-3C6186D887` now retain that exact corrected spelling. The
permanent atom ID, category, and source coordinates are unchanged. The earlier
short-i-exception interpretation was discarded before any such exception was
implemented. Both pi-pī-tza and its fully segmented pi-pī-tz-a spelling
retain long ī; pi-ī-n-a follows the normal fused-stock frequentative rule.

The focused `classical_fused_stock_frequentative_owner_exact` proof passed
24/24 on its first run: two direct and two legacy observations, both output
families, canonical contracted ni/hui sources, hyphen-free causative parity,
repetition, preterits, ordinary/mismatching stock boundaries, preserved
historical po lineage, the corrected pi forms, and copied/injected-authority
rejection. Live Class B plus nonspecific nonhuman object generated
**Nitlapipītza.** from the corrected source. The browser then generated the
same result from **pi-ī-n-a → frequentative → destockal causative**.

This formation/correction block is closed. Exact lexical meaning owners,
unresolved contracted causative ancestry, and previously recorded aggregate
evidence maintenance remain separate non-blocking backlog. No global totals,
historical ledgers, or generated evidence were refreshed.

## §27.4.3 — three exact lexical frequentative readings (build 421)

Closed the separate lexical-owner block for `ACI-P247-L037-76A99F08C2`
(po-pō-tz-a: cause something to emit smoke), `ACI-P247-L039-6DD10064FA`
(to-tō-tz-a: spur someone or something on), and
`ACI-P248-L003-3C6186D887` (corrected pi-pī-tza: cause something to dribble).
Permanent p2702/p2703/p2704 aliases now observe three separate canonical
lexical relations instead of a generic causative label or another stem's
long-vowel/target result. The two to-tō-tz-a witnesses preserve both tē and
tla participants and must share the same canonical lexical identity/readings.
The adjacent examples and documentary children received no new receipts.

The canonical frequentative operation now carries an available-reading
relation for these exact quantity-sensitive lexical identities. It binds the
owner-issued source application and its object requests, the target application
and its source analysis, and the actual conditioned predicate and participant
slots. Completed lexicalized sources and productively generated fused-stock
targets use the same relation. Ordinary preterit conditioning preserves the
lexical reading without reconstructing a lexical source from a surface ending.
Meaning remains available, not asserted by spelling: no homograph is forced
to take it, citation objects do not restrict other valid participants, and
unknown or short-stock identities borrow no meaning. Formation and Source
admission remain productive. This adds no pi shortening exception.

Result cues project the actual available reading and cite only that reading's
own atom. The first visible cue clause includes the meaning, so the normal
compact cue presentation does not hide it behind the stem label.

Final focused proof `classical_fused_stock_lexical_meanings_owner_exact` passed
25/25: three direct and three legacy observations; exact lexical and tē/tla
participant continuity; completed/generated targets; hyphen-insensitive,
quantity-sensitive pi identity; open unknown and non-cited participants;
selected applicative boundary; preterit continuity; exact per-lexeme UI atom
and negative cue checks; copied/injected authority rejection. The initial
test attempt stopped on an unsafe test dereference of an intentionally blocked
injection; the test was corrected. Review then tightened the two-witness and
UI atom/wording checks before the final passing run.

Live build `20260904-frequentative-lexical-421` generated **Nitlapipītza.**
from **pi-ī-n-a → frequentative → destockal causative** with Class B and tla.
Its expanded Result cue visibly reads “pi-pī-tz-a: available lexical reading:
cause something to dribble.” This product/owner block is closed. The wider
goal stays active; unresolved contracted causative ancestry and the previously
recorded aggregate-evidence maintenance remain separate non-blocking backlog.
No global counts, historical ledgers, or portable evidence were refreshed.

## Contracted causative ancestry into frequentative formation (build 422)

Closed a distinct inter-operation repair: typed **mī-n-a** and **xī-ni-ā**
were incorrectly producing mi-mi-tz-a and xi-xi-tz-a. Canvas §24.5.9
explicitly gives their fused ni sources and respective replacement/addition
causatives; §27.4.2 includes n-a and ni-ā, and §27.4.3 retains the fused
stock vowel. Their actual frequentative Results now preserve **mi-mī-tz-a**
and **xi-xī-tz-a**.

The canonical late-operation owner considers a retained-stock ni/hui source
only as a hypothesis. It then requires issued Class-B source machinery,
validated source analyses and causative inventory, an exact lexical fused
analysis belonging to the selected option, compatible typed causative
construction, and a quantity-sensitive forward target/class match with the
actual signed causative Source. No lexical map was duplicated and no long
vowel alone establishes fusion. The operation carries
`fusedStockFrequentativeFrame.causativeSourceAncestryFrame` with the actual
source agreement, recovered source, inventory, selected option, source analysis,
and shared coalescence relation. The inventory-owned and option-owned analysis
frames are both validated; their canonical signatures agree even though the
evaluator issues separate objects.

Explicit root-plus-stock inputs keep their existing structural path. Unknown
or short-stock inputs cannot borrow exact ancestry. In particular, cē-hu-a
does not borrow the cē-hui analysis: the latter forward-generates cē-hui-ā,
not cē-hu-a. The separate hua ancestry is not inferred here, and the hui-ā
frequentative construction is not added by analogy to §27.4.2's named inputs.
The user's long-ē corrections remain untouched.

Focused proof `classical_contracted_causative_frequentative_ancestry_exact`
passed **18/18**: exact analysis/route/construction identities, signed forward
continuity, contracted and explicit spellings, preterits, repeated formation,
real subject/object preservation, ordinary stock reduction, unknown/short and
related-hui negative boundaries, and copied/injected-authority rejection.
The first run passed all product checks but two reference-identity assertions
needed alignment with the evaluator's separately issued equivalent analyses;
the final run passed after that test correction. Live build
`20260904-contracted-ancestry-422` showed **mī-n-a → mi-mī-tz-a** and generated
**Nitlamimītza.** with Class B and a selected tla object.

This product block is closed. No new atom count or owner-adapter claim was
added, and no previously closed proof was rerun. Exact extant-source ancestry
atom receipts and the already recorded aggregate-evidence maintenance remain
separate non-blocking backlog. The wider goal remains active.

## Extant fused-source structural observations (build 423)

Closed the bounded owner-proof block for seven structural observations in
Canvas §24.5.9 (Canvas lines 7896–7898; inventory lines 11130–11150):

- `ACI-P204-L002-61A69ED581-03`: mi-ī-ni → mī-ni coalescence.
- `ACI-P204-L002-61A69ED581-05`: mī-ni → mī-n-a causative formation.
- `ACI-P204-L003-429A0E8ACE-03`: xi-ī-ni → xī-ni coalescence.
- `ACI-P204-L003-429A0E8ACE-06`: xī-ni → xī-ni-ā causative formation.
- `ACI-P204-L004-AE01B03F8B`: ce + ē + hui source constitution.
- `ACI-P204-L004-AE01B03F8B-02`: ce-ē-hui → cē-hui coalescence.
- `ACI-P204-L004-AE01B03F8B-05`: cē-hui → cē-hui-ā causative formation.

The existing `classical-destockal-irregular-source-lifecycle` owner gained
three additive source-specific selections and seven distinct proof coordinates.
Each observes its own issued lexical source analysis, actual reconstructed
source realization, or actual Source Result continued through the selected
causative. The continuation request uses the public read-only source-constituent
projection and the original issued Result; no displayed stem or formula
substitutes for provenance. Existing general-coalescence and nonextant-source
coordinates remain unchanged. This block changes proof observations, not
generated grammar output, source admission, or the user's long-ē hua/hui forms.

Focused proof `classical_extant_fused_source_owner_exact` passed **23/23**:
seven public owner receipts, three exact lexical analyses and realized
coalescences, three continued causatives with real participant continuity,
long-ē source constitution, cross-source exclusion, unclaimed neighboring
atoms, and copied-authority rejection. The initial run exposed an incorrect
test participant-choice key and omitted required continuation source fields;
both were corrected before the passing run. Build 423 is wired on disk;
no new live-browser delivery claim is made for this owner-only block.

The causative observations cover structural formation only. They deliberately
use a specific causee and do not count that as proof of the separate cited
`tla-` claims (`-06`, `-07`, and `-06`, respectively), lexical meanings,
reconstruction notation, or documentary comparisons. Those remain pending.
The stale ledger spelling `ce-e-hui` and previously recorded aggregate-evidence
issue remain separate, non-blocking factory backlog. No global counts,
historical ledger, or portable evidence were refreshed. The wider goal stays
active; this focused block is closed.

## Extant fused-source nonspecific causee observations (build 424)

Closed the three participant claims left pending by build 423:

- `ACI-P204-L002-61A69ED581-06`: the nonspecific nonhuman causee of mī-n-a.
- `ACI-P204-L003-429A0E8ACE-07`: the nonspecific nonhuman causee of xī-ni-ā.
- `ACI-P204-L004-AE01B03F8B-06`: the nonspecific nonhuman causee of cē-hui-ā.

Canvas §24.5.9 supplies the three cited tla forms (lines 7896–7898).
§24.8 (lines 8002–8004 and 8060–8079) distinguishes active-source specific
or reflexive causees from impersonal-source nonspecific causees. §23.1
(lines 7459–7486) distinguishes a causative object from an inherent direct
object. The active-source restriction therefore remains correct; no new
active causee choice or lexical direct-object shortcut was introduced.

The existing owner gained three additive impersonal-source selections and
three distinct exact coordinates. Each chooses an actual hua nonactive
option from the normal source inventory, then the exact fused type-one
causative option. That hua choice is a proof fixture, not a new default or
a restriction on other valid choices. The signed voice transformation has
a referentially empty source subject; the causative participant transform
derives a nonspecific nonhuman causee governed by the causative suffix.
The imported first-person singular subject remains independent. Ordinary
generation produces **nitlamīna**, **nitlaxīnia**, and **nitlacēhuia**.

Focused proof `classical_extant_fused_nonspecific_causee_owner_exact` passed
**18/18** on its first run after review. It covers three direct owner receipts,
actual impersonal sources, selected lexical causatives and participant roles,
independent formula/surface results, rejection of active-source nonspecific
overrides, required source-formation choice, unchanged specific/reflexive
defaults and ambiguity, cross-source and meaning non-claims, and copied
authority rejection. The private projection checks genuine issuance and
reference continuity; sanitized public receipts compare exact values and
signatures. No previously closed suite or global evidence run was repeated.
Build 424 is wired on disk; this block does not claim new browser delivery.

A separate product-readout defect was identified, not repaired here:
`getClassicalNahuatlCanvasParticipantPositions` in `derived_vnc_layers.mjs`
falls back to a synthetic direct-object position for monadic derived frames,
even though the operation and derived machinery retain the correct causative
request. The finite formula and word use the correct typed slot carrier;
the compact `participantPositions` metadata mislabels the governor and object
identity. These receipts observe the canonical operation's participant relation,
not that faulty compact label. Repairing the monadic readout is the next
distinct product block. Lexical meanings, reconstruction notation, stale
ledger spelling, and aggregate-evidence maintenance remain separate backlog.
No global counts or portable evidence were refreshed. The wider goal is active.

## Single-object derived participant readout (build 425)

Closed the signed Result-metadata defect discovered in build 424. The finite
and citation projections now preserve the actual causative/applicative object
ID, kind, person, governor, and derivational level from the issued derivation.
Previously, compact single-object Valence slots triggered a generic fallback
that relabeled the object as `source-object-1` governed by `directive`.
Canvas §23.1 distinguishes these object roles; the grammar operation already
held the correct distinction, so no new grammatical choice was needed.

`getClassicalNahuatlCanvasParticipantPositions` retains the existing cluster
and explicit typed-position paths. For a single-object derived frame it now
requires the exact operation-owned target-request array, then finds the unique
monadic/dyadic Valence slot and projects that slot's real carrier and sounded
state. A directional such as on is another slot, not another object; it does
not disable the corrected projection. Generic direct-object behavior is
unchanged. The impersonal tē-before-retained-ne note now requires that actual
two-object configuration; a lone tla causee does not receive it accidentally.

Focused proof `classical_monadic_derived_participant_readout_exact` passed
**18/18** on its first run. It checks canonical finite and citation issuance,
impersonal/specific/reflexive causees, an applicative object, the extra
directional slot, unchanged direct and multiple-object paths, genuine tē+ne
ordering, independent and unchanged formulas/words, and copied/tampered
authority rejection. No earlier completed proof or global audit was rerun.

Live build 425 visibly generated **Nitlamīna.** with mī-ni, impersonal Source,
selected mī-nī-hua formation, the exact type-one causative, and causer 1sg.
Its existing visible causee cue was already correct: the repaired metadata
has no direct browser-visible consumer. The final refresh after adding the
directional-slot boundary could not be confirmed because browser control
timed out. The existing Python server was independently confirmed listening
on 127.0.0.1:8090 (PID 16511); it was not restarted. This delivery uncertainty
does not replace the successful focused runtime proof or establish a server
failure. Browser refresh confirmation remains separate follow-up.

The subsequent delivery check found the actual tab titled **This page
crashed**. Browser automation could not acquire its generated crash screen
because that screen uses a blocked data URL. Opening the original localhost
application in a fresh tab then returned **target closed while handling
command**. No successful reload is claimed, and the server was not restarted.
This is browser-delivery evidence, not evidence of a grammar regression or a
diagnosed cause of the crash.

No atom count, historical ledger, or portable evidence was refreshed. The
remaining lexical meanings, reconstruction notation, stale ledger spelling,
and aggregate-evidence maintenance remain non-blocking backlog. The wider
goal stays active; the single-object metadata repair is closed.

## Extant destockal lexical-reading availability (build 426)

Added the eleven available lexical readings in Canvas §24.5.9 (lines
7896–7898) to the ordinary VNC application interpretation boundary. The new
`buildClassicalNahuatlExtantDestockalMeaningFrame` accepts an actual issued
Application, not example text or caller-supplied meanings. Its validator
retains issuance identity. Lexical licensing observes the exact issued
analysis and its vowel quantities; it does not restrict admission or grammar
generation for other stems.

The source readings bind the actual Source subject. Exact type-one
causative readings bind the operation-owned causee and its actual projected
participant. In the cited impersonal-source applications this is the implicit
agent becoming the nonspecific nonhuman causee, not promotion of the discarded
referentially empty Source subject. Causative frames expose only their causal
readings, not source readings relabeled as target participants.

All readings remain **available-not-asserted**. Wall, mountainside, fire,
stitches, and fire-or-flame interpretations carry explicit unresolved context
conditions. Neither third-person agreement nor tla supplies those referents.
No new UI choice, automatic translation, contextual assertion, or morphology
change is introduced. Context resolution itself is outside this availability
block.

The existing permanent owner now directly observes eleven bound reading
relations: mīni atoms `-04/-07`, xīni `-04/-05/-08/-09`, and cēhui
`-03/-04/-07/-08/-09` on their respective ACI-P204 parent IDs. Source readings
use the existing extant-source selectors; the cited tla readings use the
existing impersonal-causee selectors. Old structural coordinates are retained.
Each new reading observation includes its actual participant binding, not
merely a completion flag or glossary label.

Focused proof `classical_extant_destockal_lexical_meanings_owner_exact` passed
**22/22** on its first run. The same run confirmed that both new functions are
published by the normal installed runtime. It covers all eleven exact owner
receipts, actual subject/causee continuity, unresolved context, unchanged
independent formulas and words, copied authority rejection, and admission of
unknown and short-vowel sources without borrowed lexical meanings. No earlier
completed suite or aggregate audit was rerun.

The local browser entry/cache chain is build 426. Browser delivery remains
unconfirmed after the separately recorded crash and failed fresh-tab recovery;
no new live-browser result is claimed. Reconstruction notation, noun lexical
relations, contextual assertion, stale ledger spelling, and aggregate evidence
remain separate non-blocking backlog. No global totals, historical ledger,
portable evidence, commit, or deployment was updated. This availability block
is closed; the wider goal remains active.

## Context-supported destockal reading selection (build 427)

Completed the separate context step for the five conditional §24.5.9 readings:
wall → collapse; mountainside → slide/collapse; fire → go out; stitches → rip
out; and fire/candle flame/flame → extinguish. These are stronger observations
of five existing atoms, not five new atoms or an increased denominator.

`interpretClassicalNahuatlExtantDestockalReading` consumes an issued build-426
meaning frame, optional supplied context, and an optional exact meaning ID.
Context must contain the identical canonical participant object already bound
by that meaning frame. A matching name, copied participant, foreign subject,
causer, or discarded source subject cannot substitute for the actual referent.
The interpreter checks the actual Source subject or operation-owned causee;
it does not reconstruct roles from carriers or formula text.

Supplied context supports readings but never selects one automatically.
Selection requires an explicit supported meaning ID. Missing or mismatched
context blocks that narrow selection while retaining other supported general
readings. Even an explicitly selected reading is recorded as
`selected-not-truth-asserted`: supplied context is not independent evidence
that the event actually occurred. The build-426 availability frame remains
unchanged and all formula/surface output still comes from its canonical Result.

Literal nonhuman context cannot overwrite an explicitly human participant.
It introduces no additional grammatical animacy or number restriction:
default animate/unspecified-humanness 3sg remains usable without changing its
features, and English plural “stitches” does not force Classical plural
morphology. The source owner fixtures use explicit nonanimate/common-number
participants as one concrete witness, not as an admission rule.

The five new contextual-selection coordinates retain the same permanent
owner and all existing availability/structural coordinates. Their canonical
values observe issued contextual Results, selected reading objects, matched
conditions, exact participant bindings, and unchanged output. The extinguish
coordinate observes fire, candle-flame, and flame separately.

Focused proof `classical_extant_destockal_contextual_readings_owner_exact`
passed **28/28**. It covers five public owner receipts, raw canonical issuance,
context without automatic selection, explicit supported selections, retained
general readings, exact mountainside matching, wrong/copy/foreign/causer
participants, invalid selectors and authority fields, human-fact conflicts,
and unchanged participants/formulas/words. The same run confirmed both new
interpreter functions on the ordinary installed runtime.

The first attempt stopped at a test-context TypeError because the private
irregular-source observation builder is not a public capability. The completed
run supplied those two private builder/validator dependencies from their
exported factory in test setup only. No runtime capability was exposed to fix
that apparatus issue, and no already completed proof was rerun.

The local entry/cache chain is build 427. No UI control or automatic
translation was added, and live browser delivery remains unconfirmed after
the earlier recorded crash; this block does not claim browser recovery.
Reconstruction notation, noun lexical relations, stale ledger spelling, and
aggregate evidence remain separate backlog. No global counts, historical
evidence, portable disk, commit, or deployment were changed. This context
selection block is closed; the wider goal remains active.

## xīni versus short xi-ni lexical identity (build 428)

A targeted reconstruction review found new observable evidence of a quantity
leak. Before the repair, direct `xi-ni` was correctly realized as **xini** but
received the exact xīni fused analysis, underlying segments `xi + ī + ni`, and
the collapse-wall/mountainside meanings. The exact causative table also
accepted short `xi-ni`/`xini` as aliases for the long-vowel exception. This was
not a failure of productive Source admission; it was inappropriate borrowing
of an exact lexical analysis and operation across vowel quantities.

Removed only `xi-ni` and `xini` from the xīni source-analysis witness and exact
causative alternation in `vnc_derivation_evaluator.mjs`. Both retain
`xī-ni`/`xīni`. The existing lexical key already ignores editorial hyphens
while preserving vowel quantity, so no new matching machinery or grammar
restriction was needed. Canvas §24.5.9 explicitly gives `*(xi-ī-ni) > (xī-ni)`
and the long-vowel `xī-ni-ā` causative.

Focused proof `classical_xini_quantity_identity_exact` passed **14/14**.
Short hyphenated and unhyphenated inputs remain authorized and realize
**xini**; their generic causative inventories remain open, without the exact
long-vowel rule or its lexical readings. The two long spellings retain the
actual exact analysis, causative option, meanings, and
`#ni-0+tla(xī-ni-a)0+0-0#` / **nitlaxīnia**. Context cannot recover the long
meaning from a short source. One existing owner receipt confirms unchanged
long-quantity metadata, and copied authority remains rejected.

The first focused run had 12/14 checks pass; the other two incorrectly read
an analysis-category field directly from the option. The corrected assertions
read that category from the actual option-linked Source analysis. No further
product change was required, and no previously completed suite was rerun.

The originally selected notation work remains separate: mini and xini `-02`
need explicit reconstruction-status observations, and cēhui's constitution
still needs the distinct historical-notation relation. Existing coalescence
and the separate po/to nonextant lifecycle are not reopened. The asterisk
alone must not be treated as a universal nonextant/ungrammatical marker.

The local browser/cache chain is build 428. Browser delivery is still
unconfirmed after the earlier crash; no new browser result is claimed. Noun
lexical relations, stale ledger spelling, and aggregate evidence also remain
separate backlog. No atom denominator, global registry, historical evidence,
portable disk, commit, or deployment changed. The quantity repair is closed;
the wider goal remains active.

## Extant destockal reconstruction notation (build 429)

Canvas §24.5.9 distinguishes the reconstructed underlying sources
`*(mi-ī-ni)`, `*(xi-ī-ni)`, and `*(ce-ē-hui)` from their extant fused stems
`mī-ni`, `xī-ni`, and `cē-hui`. The asterisk here is not an assertion of
ungrammaticality or independent nonexistence. The separate prose about
nonextant po/to sources retains its already closed lifecycle relation.

The three exact lexical analyses now carry a frozen reconstruction-notation
frame linked to their actual root → stock → stem structure. The canonical
Source-analysis signature and validator include it. The ordinary Source
constitution projection preserves that exact analysis/frame and provides a
read-only explanation; this annotation cannot admit a source or generate a
formula or word. No generic asterisk parser, new user choice, or grammar
realization rule was added.

The existing irregular-source owner binds the annotation to its issued
Source applications and actual coalescence relation. Mini and xini receive
their missing `ACI-P204-L002-61A69ED581-02` and
`ACI-P204-L003-429A0E8ACE-02` observations. The distinct historical relation
for cēhui is nested in its existing source-constitution coordinate, without
inventing a duplicate atom or increasing a denominator.

Focused proof `classical_extant_destockal_reconstruction_notation_exact`
passed **12/12**. It covers the three owner observations, exact signed
analysis/structure identity, the ordinary installed Source projection,
actual reconstruction/coalescence links, hyphen parity, short/open input,
starred/caller-injected notation, and unchanged formulas, words, participants,
and authority boundaries. The first run was 11/12: one test incorrectly
expected starred `ce-ē-hui` to lose the note already licensed by that exact
unstarred source. The repaired assertion proves starred/unstarred parity and
no annotation for a starred unknown source. No parser or additional product
change was made, and no completed historical suite was rerun.

The local entry/cache chain is build 429. This notation block is closed.
The current composer consumes the Source structure parts, not the projection's
explanation text; this block does not claim a new visible browser note.
Browser delivery remains unconfirmed after the recorded crash. Noun lexical
relations, stale ledger spelling, and aggregate evidence are separate backlog.
No global scan, historical-proof rerun, portable-disk work, commit, or
deployment was performed. The wider goal remains active.

## cēhui / ce-tl lexical root relation (build 430)

Canvas §24.5.9 gives `(ce)-tl-`, “ice/icicle,” in the lexical source note for
`*(ce-ē-hui) > (cē-hui)`. Short **ce** is the shared root/nounstem; long
**cē** is the fused VNC stock. Atoms `ACI-P204-L004-AE01B03F8B-10/-11`
previously had no effective owner observations for this lexical relation.
Neither the passage nor those atoms establish grammatical animacy, a plural
formation, or a productive denominal operation.

The ordinary application now issues a cross-source lexical-relation frame
from an exact cēhui Source meaning frame and an actual ordinary NNC Result
for short `ce`, tl-class. Its two noun readings are “ice” and “icicle,” both
available but unselected. Canonical identity, root equality, noun class, and
noncompound source structure are checked; no subclass, animacy, or number
is inferred. The relation cannot authorize Source admission, generate words,
or assert a whole-clause translation. Ordinary NNC source admission,
lexicon, signatures, operation choices, and realization remain unchanged.

The existing irregular-source owner has one new dedicated nominal selection.
Atom -10 observes the whole shared-root relation; atom -11 observes its
icicle reading linked to the issued noun Source and Result. Existing owner
IDs and coordinates remain permanent, and this selection does not rerun the
old causative/coalescence proof path. The observation's third-common NNC
coordinate is a fixture, not a lexical claim about ice or icicles.

Focused proof `classical_cehui_nominal_root_relation_exact` passed **14/14**
in one run. It covers both exact owner observations, raw issued identities,
short-root quantity, the independent `#0-0(ce)tl-0#` / **cetl** result,
unselected ice/icicle readings, authority boundaries, copied and injected
inputs, long `cē`, other noun classes, compounds, unrelated VNC meanings,
and continued open NNC admission. The same run confirmed both new functions
on the ordinary installed runtime, not only an inspection/test context.
Before the run, static fixture review removed an unsupported scalar-operation
`animacy` input: the actual operation derives it from `subject: 3common`.
No noun-engine change or earlier proof rerun was needed.

This lexical-relation block is closed. The local entry/cache chain is build 430;
live browser delivery is not claimed. Stale ledger spelling and aggregate
evidence remain separate non-blocking factory items. No global counts,
historical-proof reruns, portable-disk work, commit, or deployment were done.
The wider goal remains active.

## Live browser delivery recovered (build 430)

The existing Python server, PID 16511, remains alive on `127.0.0.1:8090`
with the exact fast workspace as its working directory. The served entry,
main module, and bootstrap returned HTTP 200 and carried build
`20260904-cehui-nominal-root-430`. No server restart was needed.

The old in-app tab was still on its crash page; browser policy prevented
automated reload of that internal error page. A fresh tab in the same browser
successfully loaded the ordinary local URL and completed startup. The rendered
workbench confirmed build 430, and no browser errors were reported.

One focused live interaction used the normal typed Source field, class B,
and Use this Source / Enter. `ce-ē-hui` produced **Nicēhui.** with
`#ni-0(ce-ē-hui)0+0-0#`; replacing it with retained `ce-ē-hua` produced
**Nicēhua.** with `#ni-0(ce-ē-hua)0+0-0#`. The visible Source, Grammar,
generated form, linear format, diagram, and sentence formula were inspected.
This verifies delivery and the immediate hua/hui boundary, not a rerun of
the completed grammar suites or a claim of whole-repository readiness.

The fresh working tab was shown and retained as the live deliverable, ending
at `http://127.0.0.1:8090/#classical/v1/vnc/(ce-%C4%93-hua)/tr/intransitive`.
The old crash tab was not deleted. No product files, security settings,
server state, global evidence, or portable disk were changed. This browser
delivery block is closed. Source-explanation UI work, stale ledger spelling,
and aggregate evidence remain separately scoped; the wider goal stays active.
