"use strict";const fs=require("fs"),path=require("path");const{createSuite}=require("./runner");const ROOT=path.resolve(__dirname,"..","..");function run(ctx={}){const s=createSuite("classical_lesson53_reader_guidance");const ledger=JSON.parse(fs.readFileSync(path.join(ROOT,"docs/canvas-progress/lesson53-review-ledger.json"),"utf8"));const plan=JSON.parse(fs.readFileSync(path.join(ROOT,"docs/canvas-progress/lesson53-review-plan.json"),"utf8"));const records=ledger.records.filter(record=>record.implementationCredit==="EXACTLY_OBSERVED"&&record.reviewStatus==="IMPLEMENTATION_PROVEN");const ideas=ctx.LESSON53_READER_GUIDANCE_GROUPS,panel=ctx.ClassicalAuthorityPanel(),start=panel.indexOf('data-classical-reader-guidance-lesson="53"'),html=panel.slice(start,panel.indexOf("</details>",start)+10);s.eq("Lesson 53 guidance covers the exact technical-proof groups",{guidance:ideas.map(group=>group.ideaId),plan:plan.groups.map(group=>group.groupId),ledgerGroups:[...new Set(records.map(record=>record.reviewGroupId))],atoms:records.length},{guidance:plan.groups.map(group=>group.groupId),plan:plan.groups.map(group=>group.groupId),ledgerGroups:plan.groups.map(group=>group.groupId),atoms:406});for(const group of ideas){const rows=records.filter(record=>record.reviewGroupId===group.ideaId);s.ok(`Lesson 53 ${group.ideaId} guidance is delivered`,rows.length>0&&rows.every(record=>record.readerGuidanceIdeaId===group.ideaId&&record.readerObservationTest.includes("classical_lesson53_reader_guidance.test.js"))&&html.includes(`data-classical-reader-guidance-group="${group.ideaId}"`));}s.ok("Lesson 53 guidance remains non-authoritative",!html.includes('grammarAuthority="true"')&&ledger.authority.reviewLedgerAuthorizesGrammar===false&&ctx.isLesson53ReaderGuidanceExact(ideas));return s;}module.exports={run};
