const idea=(ideaId,title,guidance)=>Object.freeze({ideaId,title,guidance});export const LESSON54_READER_GUIDANCE_GROUPS=Object.freeze([
idea("lesson54-denominal-domain","Denominal VNCs begin with typed nominal Sources","One shared owner licenses verbalization, projects participants, realizes class and boundary, and issues the finite VNC Result."),
idea("lesson54-inceptive-stative-suffixes","Inceptive and stative routes retain Source rank","ti, hui, ya, a, and hua preserve root or stem rank, class, perfective, and boundary evidence rather than reading them from spelling."),
idea("lesson54-included-possessor-ti","Included possessors remain Source participants","Exact possessive NNC Sources carry their possessor into the derived VNC participant graph; hidden state cannot add or remove it."),
idea("lesson54-possession-ti","Homophonous ti operations stay distinct","Possession ti and inceptive ti remain different licensed operations even when their written suffix is the same."),
idea("lesson54-ti-causatives","Causatives continue exact ti Results","lia, a, ia, and tla routes consume exact upstream operation frames and derive new object structure canonically."),
idea("lesson54-ti-applicatives","Applicatives preserve ti provenance","A licensed ti Result can continue into an applicative while keeping Source identity, valence history, class, and boundary realization."),
]);export function isLesson54ReaderGuidanceExact(candidate=[]){return JSON.stringify(candidate)===JSON.stringify(LESSON54_READER_GUIDANCE_GROUPS);}export function renderLesson54ReaderGuidance(escapeHtml=String){return `<details class="classical-reader-guidance__lesson" data-classical-reader-guidance-lesson="54"><summary class="classical-reader-guidance__lesson-summary"><span>Lesson 54</span><small>Denominal verbstems, part one</small></summary><div class="classical-reader-guidance__lesson-body"><div class="classical-reader-guidance__grid">${LESSON54_READER_GUIDANCE_GROUPS.map(group=>`<section class="classical-reader-guidance__card" data-classical-reader-guidance-group="${escapeHtml(group.ideaId)}"><h5 class="classical-reader-guidance__title">${escapeHtml(group.title)}</h5><p data-classical-reader-guidance-idea="${escapeHtml(group.ideaId)}">${escapeHtml(group.guidance)}</p></section>`).join("")}</div></div></details>`;}
